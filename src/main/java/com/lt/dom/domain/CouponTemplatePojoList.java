package com.lt.dom.domain;

public class CouponTemplatePojoList {

    private int page; //页码，默认值为 "1"，取值范围：1-100000000。

    private int per_page; // 每页数量，默认值为 "10"，取值范围：1-100。

   // private int created[gt]; // 创建时间大于该值，用 Unix 时间戳表示。

   // private int created[gte]; // 创建时间大于或等于该值，用 Unix 时间戳表示。

  //  private int created[lt]; //创建时间小于该值，用 Unix 时间戳表示。
  //  private int created[lte]; //创建时间小于或等于该值，用 Unix 时间戳表示。



}
